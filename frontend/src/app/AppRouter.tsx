import React from "react";

import { HomePage } from "../pages/home/HomePage";

// Application router (currently only one page)
export const AppRouter = () => {
  // Currently only one page
  return <HomePage />;
};
